# Task 1: Basic R

Welcome to your first agent exercise!

Start a conversation with the Gemini CLI or OpenCode agent in this directory.

**Your goal:** Ask the agent to calculate the mean `mpg` for different `cyl` groups in the built-in `mtcars` dataset and create a simple visualization using `ggplot2`.
