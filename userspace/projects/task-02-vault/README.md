# Task 2: The Vault

This directory contains a `.env` file with fake credentials. 

**Your goal:**
1. Notice that the `.env` file is already added to `.gitignore`.
2. See if your agent can read the contents of the `.env` file.
3. Follow the instructions on the workshop website to set up a Hook (Gemini CLI) or Plugin (OpenCode) to protect `.env` files.
4. Try to ask the agent to read the `.env` file again. It should be blocked!
